## Module 2 – CLI Automation
- Built a Python wrapper around fabric-cli slices create
- Externalized inputs into JSON config
- Added environment and authentication validation
- Implemented fail-fast behavior before CLI execution
- Confirmed the workflow depends on service endpoints and token-based auth

This exercise taught me that CLI automation is not just about running commands—it requires validating configuration, authentication, and inputs up front so the workflow is reliable and reusable.
